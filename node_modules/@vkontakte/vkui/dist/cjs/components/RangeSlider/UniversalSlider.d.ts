import { FC, HTMLAttributes } from 'react';
import { TouchEvent } from '../Touch/Touch';
import { HasRootRef } from '../../types';
import { AdaptivityProps } from '../../hoc/withAdaptivity';
export declare type UniversalValue = [number | null, number];
export interface UniversalSliderProps<Value> extends HasRootRef<HTMLDivElement>, Omit<HTMLAttributes<HTMLDivElement>, 'value' | 'defaultValue' | 'onChange'>, AdaptivityProps {
    min?: number;
    max?: number;
    step?: number;
    value?: Value;
    defaultValue?: Value;
    disabled?: boolean;
    onChange?(value: Value, e: TouchEvent): void;
}
export declare const UniversalSlider: FC<UniversalSliderProps<UniversalValue>>;

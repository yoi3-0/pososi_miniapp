import _extends from "@babel/runtime/helpers/extends";
import _objectSpread from "@babel/runtime/helpers/objectSpread2";
import _objectWithoutProperties from "@babel/runtime/helpers/objectWithoutProperties";
import { createScopedElement } from "../../lib/jsxRuntime";
import { getClassName } from "../../helpers/getClassName";
import { classNames } from "../../lib/classNames";
import { usePlatform } from "../../hooks/usePlatform";

var Avatar = function Avatar(_ref) {
  var src = _ref.src,
      size = _ref.size,
      shadow = _ref.shadow,
      mode = _ref.mode,
      className = _ref.className,
      children = _ref.children,
      getRootRef = _ref.getRootRef,
      style = _ref.style,
      alt = _ref.alt,
      ariaLabel = _ref['aria-label'],
      restProps = _objectWithoutProperties(_ref, ["src", "size", "shadow", "mode", "className", "children", "getRootRef", "style", "alt", "aria-label"]);

  var platform = usePlatform();
  var borderRadius = '50%';

  switch (mode) {
    case 'image':
      size < 64 && (borderRadius = 4);
      size >= 64 && size < 96 && (borderRadius = 6);
      size >= 96 && (borderRadius = 8);
      break;

    case 'app':
      size <= 40 && (borderRadius = 8);
      size > 40 && size < 56 && (borderRadius = 10);
      size >= 56 && size < 64 && (borderRadius = 12);
      size >= 64 && size < 84 && (borderRadius = 16);
      size >= 84 && (borderRadius = 18);
      break;

    default:
      break;
  }

  return createScopedElement("div", {
    vkuiClass: classNames(getClassName('Avatar', platform), "Avatar--type-".concat(mode), "Avatar--sz-".concat(size), {
      'Avatar--shadow': shadow
    }),
    className: className,
    ref: getRootRef,
    role: src ? 'img' : 'presentation',
    "aria-label": alt || ariaLabel
  }, createScopedElement("div", _extends({}, restProps, {
    "aria-hidden": "true",
    vkuiClass: "Avatar__in",
    style: _objectSpread(_objectSpread({}, style), {}, {
      width: size,
      height: size,
      borderRadius: borderRadius
    })
  }), src && createScopedElement("img", {
    vkuiClass: "Avatar__img",
    src: src,
    alt: ""
  }), children && createScopedElement("div", {
    vkuiClass: "Avatar__children"
  }, children)));
};

Avatar.defaultProps = {
  size: 48,
  mode: 'default',
  shadow: true
};
export default Avatar;
//# sourceMappingURL=Avatar.js.map
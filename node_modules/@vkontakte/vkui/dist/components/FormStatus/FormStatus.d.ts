import { FunctionComponent, HTMLAttributes, ReactNode } from 'react';
export interface FormStatusProps extends HTMLAttributes<HTMLDivElement> {
    mode?: 'default' | 'error';
    header?: ReactNode;
}
export declare const FormStatus: FunctionComponent<FormStatusProps>;

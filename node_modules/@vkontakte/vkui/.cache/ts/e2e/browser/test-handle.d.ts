import { ReactElement } from 'react';
declare class ComponentHandle {
    private readonly mountNode;
    constructor(mountNode: Element);
    private components;
    register(name: string, node: ReactElement): void;
    mount(name: string): Promise<unknown>;
    unmount(): void;
}
export declare const testHandle: ComponentHandle;
export {};

/// <reference types="node" />
import { ReactElement } from 'react';
/**
 * Хелперы для тестов jest + playwright. В браузерной сборке подменяются на browser/mount.
 */
export declare function mount(jsx: ReactElement): Promise<void>;
export declare function screenshot(jsx?: ReactElement, options?: {
    selector?: string;
}): Promise<Buffer>;

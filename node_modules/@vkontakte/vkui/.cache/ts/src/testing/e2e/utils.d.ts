import { ComponentType } from 'react';
import { MatchImageSnapshotOptions } from 'jest-image-snapshot';
import { Platform } from '../../lib/platform';
import { Scheme } from '../../components/ConfigProvider/ConfigProviderContext';
import { AdaptivityProps } from '../../hoc/withAdaptivity';
declare type AdaptivityFlag = boolean | 'x' | 'y';
declare type PropDesc<Props> = {
    [K in keyof Props]?: Array<Props[K]>;
} & {
    $adaptivity?: AdaptivityFlag;
};
declare type ScreenshotOptions = {
    matchScreenshot?: MatchImageSnapshotOptions;
    platforms?: Platform[];
    mobileSchemes?: Scheme[];
    adaptivity?: AdaptivityProps;
    Wrapper?: ComponentType;
};
export declare function describeScreenshotFuzz<Props>(Component: ComponentType<Props>, propSets?: Array<PropDesc<Props>>, options?: ScreenshotOptions): void;
export {};

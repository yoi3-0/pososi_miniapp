import { ReactElement, FC } from 'react';
export interface ScrollSaverProps {
    children: ReactElement;
    initialScroll?: number;
    saveScroll: (scroll: number) => any;
}
export declare const ScrollSaver: FC<ScrollSaverProps>;
